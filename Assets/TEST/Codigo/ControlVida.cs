using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ControlVida : MonoBehaviour
{
	/*void OnTriggerEnter2D(Collider2D colisionador)
	{
		if (colisionador.gameObject.tag == "Player")
		{
			ControlJugador control = colisionador.GetComponent<ControlJugador>();
			if (control != null)
			{
				control.ActualizarVida(-1);
				Destroy(gameObject);
			}
		}
	}*/
}
